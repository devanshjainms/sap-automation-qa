# Azure OpenAI Configuration (Required)
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
AZURE_OPENAI_API_KEY=your-api-key-here
AZURE_OPENAI_DEPLOYMENT=gpt-4o
AZURE_OPENAI_API_VERSION=2024-02-15-preview

# Azure Managed Identity (Optional - for KeyVault access)
# If VM has system-assigned MI, leave blank. For user-assigned MI, provide client ID.
AZURE_CLIENT_ID=

# Logging
LOG_LEVEL=INFO

# Note: SSH keys are fetched from KeyVault at runtime.
# KeyVault name is read from sap-parameters.yaml in the workspace.
# Ensure VM's Managed Identity has "Key Vault Secrets User" role on the KeyVault.
