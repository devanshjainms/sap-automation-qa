# Copyright (c) Microsoft Corporation.
# Licensed under the MIT License.

# /*---------------------------------------------------------------------------
# |       Restore Backup to HANA DB using Azure Backup                        |
# +--------------------------------------------------------------------------*/

- name:                                 "Test Setup Tasks"
  ansible.builtin.include_tasks:        "roles/misc/tasks/test-case-setup.yml"
  run_once:                             true

# /*---------------------------------------------------------------------------
# |                          Pre Validations                                  |
# +--------------------------------------------------------------------------*/
- name:                              "Pre Validations: Backup Restore to Database"
  when:                              node_tier == "hana"
  block:
    - name:                          "Pre Validation: Verify backup vault connectivity"
      delegate_to:                   localhost
      azure_backup_hana:
        operation:                   "discover_protected_items"
        vault_resource_id:           "{{ backup_vault_resource_id }}"
        subscription_id:             "{{ azure_instance_metadata.json.compute.subscriptionId }}"
        msi_client_id:               "{{ user_assigned_identity_client_id | default('') }}"
      register:                      pre_backup_discovery
      run_once:                      true
      failed_when:                   pre_backup_discovery.protected_items | default([]) | length == 0

    - name:                          "Pre Validation: HA cluster status"
      when:                          database_high_availability | default(false) | bool
      block:
        - name:                      "Pre Validation: Run standard DB pre-validations"
          ansible.builtin.include_tasks: "roles/misc/tasks/pre-validations-db.yml"

        - name:                     "Pre Validation: Set primary/secondary from cluster status"
          ansible.builtin.set_fact:
            backup_primary_node:    "{{ cluster_status_pre.primary_node }}"
            backup_secondary_node:  "{{ cluster_status_pre.secondary_node }}"
          run_once:                 true

        - name:                     "Pre Validation: Resolve restore target for HSR"
          ansible.builtin.set_fact:
            backup_restore_vm_name:        "{{ hostvars[backup_primary_node].azure_instance_metadata.json.compute.name }}"
            backup_restore_vm_rg:          "{{ hostvars[backup_primary_node].azure_instance_metadata.json.compute.resourceGroupName }}"
            backup_restore_subscription:   "{{ hostvars[backup_primary_node].azure_instance_metadata.json.compute.subscriptionId }}"
          run_once:                 true

        - name:                     "Pre Validation: Build HSR restore parameters"
          ansible.builtin.set_fact:
            backup_restore_target_container: >-
              VMAppContainer;Compute;{{ backup_restore_vm_rg }};{{ backup_restore_vm_name }}
            backup_restore_target_db: >-
              SAPHanaDatabase;{{ db_sid | lower }};{{ backup_item_name.split(';')[-1] }}
          run_once:                 true

        - name:                     "Pre Validation: Log HSR restore targets"
          ansible.builtin.debug:
            msg: >-
              HSR restore target: container={{ backup_restore_target_container | trim }},
              db={{ backup_restore_target_db | trim }}
          run_once:                 true

    - name:                         "Pre Validation: Set standalone node facts"
      ansible.builtin.set_fact:
        backup_primary_node:        "{{ ansible_hostname }}"
        backup_secondary_node:      ""
      run_once:                     true
      when:                         not (database_high_availability | default(false) | bool)

    - name:                         "Pre Validation: Override status if no backup items found"
      ansible.builtin.set_fact:
        pre_validations_status:     "{{ 'FAILED' if (
                                        pre_backup_discovery.protected_items | length == 0
                                      ) else pre_validations_status | default('PASSED') }}"
      run_once:                     true

# /*---------------------------------------------------------------------------
# |                          Test Execution                                   |
# +--------------------------------------------------------------------------*/
- name:                             "Test Execution: Restore backup to HANA DB"
  when:
                                    - node_tier == "hana"
                                    - pre_validations_status == "PASSED"
  become:                           true
  block:
    - name:                         "Test Execution: Start timer"
      ansible.builtin.set_fact:
        test_execution_start:       "{{ now(utc=true, fmt='%Y-%m-%d %H:%M:%S') }}"
        test_execution_hostname:    "{{ ansible_hostname }}"
      run_once:                     true

    - name:                         "Test Execution: HSR - Stop HANA on secondary node"
      become_user:                  "{{ db_sid | lower }}adm"
      ansible.builtin.command:      /usr/sap/{{ db_sid | upper }}/HDB{{ db_instance_number }}/HDB stop
      register:                     hsr_secondary_stop
      delegate_to:                  "{{ backup_secondary_node }}"
      run_once:                     true
      changed_when:                 hsr_secondary_stop.rc == 0
      failed_when:                  false
      when:                         database_high_availability | bool

    - name:                         "Test Execution: HSR - Unregister secondary from replication"
      become_user:                  "{{ db_sid | lower }}adm"
      ansible.builtin.command:      /usr/sap/{{ db_sid | upper }}/HDB{{ db_instance_number }}/hdbnsutil -sr_unregister
      register:                     hsr_unregister_result
      delegate_to:                  "{{ backup_secondary_node }}"
      run_once:                     true
      changed_when:                 hsr_unregister_result.rc == 0
      failed_when:                  false
      when:                         database_high_availability | bool

    - name:                         "Test Execution: HSR - Disable replication on primary"
      become_user:                  "{{ db_sid | lower }}adm"
      ansible.builtin.shell: |
                                    set -o pipefail
                                    source /usr/sap/{{ db_sid | upper }}/home/.sapenv.sh && \
                                    hdbnsutil -sr_disable
      args:
        executable:                 /bin/bash
      register:                     hsr_disable_result
      delegate_to:                  "{{ backup_primary_node }}"
      run_once:                     true
      changed_when:                 hsr_disable_result.rc == 0
      failed_when:                  false
      when:                         database_high_availability | bool

    - name:                         "Test Execution: Stop HANA DB before restore"
      become_user:                  "{{ db_sid | lower }}adm"
      ansible.builtin.shell: |
                                    set -o pipefail
                                    source /usr/sap/{{ db_sid | upper }}/home/.sapenv.sh && \
                                    HDB stop
      args:
        executable:                 /bin/bash
      register:                     hana_stop_result
      delegate_to:                  "{{ backup_primary_node }}"
      run_once:                     true
      changed_when:                 hana_stop_result.rc == 0
      failed_when:                  false

    - name:                         "Test Execution: Trigger restore to database"
      delegate_to:                  localhost
      azure_backup_hana:
        operation:                  "restore_to_database"
        vault_resource_id:          "{{ backup_vault_resource_id }}"
        subscription_id:            "{{ azure_instance_metadata.json.compute.subscriptionId }}"
        msi_client_id:              "{{ user_assigned_identity_client_id | default('') }}"
        container_name:             "{{ backup_container_name }}"
        item_name:                  "{{ backup_item_name }}"
        restore_point_time:         "{{ backup_restore_point_time | default('') }}"
        restore_mode:               "{{ 'AlternateWorkloadRestore' if (database_high_availability | bool) else '' }}"
        target_container_name:      "{{ (backup_restore_target_container | trim) if (database_high_availability | bool) else '' }}"
        target_database_name:       "{{ (backup_restore_target_db | trim) if (database_high_availability | bool) else '' }}"
        source_resource_id:         >-
          {{ '/subscriptions/' ~ backup_restore_subscription ~ '/resourceGroups/' ~ backup_restore_vm_rg ~ '/providers/Microsoft.Compute/virtualMachines/' ~ backup_restore_vm_name
             if (database_high_availability | bool) else '' }}
      register:                     restore_to_db_result
      run_once:                     true

    - name:                         "Test Execution: Wait for restore job to complete"
      delegate_to:                  localhost
      azure_backup_hana:
        operation:                  "check_restore_job"
        vault_resource_id:          "{{ backup_vault_resource_id }}"
        subscription_id:            "{{ azure_instance_metadata.json.compute.subscriptionId }}"
        msi_client_id:              "{{ user_assigned_identity_client_id | default('') }}"
        restore_job_id:             "{{ restore_to_db_result.restore_job.job_id }}"
        poll_interval_seconds:      "{{ backup_poll_interval | default(30) }}"
        poll_timeout_seconds:       "{{ backup_poll_timeout | default(7200) }}"
      register:                     restore_job_status
      run_once:                     true
      when:                         restore_to_db_result.restore_job.job_id | default('') | length > 0

    - name:                         "Test Execution: Start HANA DB after restore"
      become_user:                  "{{ db_sid | lower }}adm"
      ansible.builtin.shell: |
                                    set -o pipefail
                                    source /usr/sap/{{ db_sid | upper }}/home/.sapenv.sh && \
                                    HDB start
      args:
        executable:                 /bin/bash
      register:                     hana_start_result
      delegate_to:                  "{{ backup_primary_node }}"
      run_once:                     true
      changed_when:                 hana_start_result.rc == 0
      failed_when:                  false

    - name:                         "Test Execution: Validate HANA is running"
      become_user:                  "{{ db_sid | lower }}adm"
      ansible.builtin.shell: |
                                    set -o pipefail
                                    source /usr/sap/{{ db_sid | upper }}/home/.sapenv.sh && \
                                    sapcontrol -nr {{ db_instance_number }} -function GetProcessList
      args:
        executable:                 /bin/bash
      register:                     hana_process_check
      delegate_to:                  "{{ backup_primary_node }}"
      run_once:                     true
      retries:                      50
      delay:                        10
      until:                        hana_process_check.rc == 0 or hana_process_check.rc == 3
      failed_when:                  false

    - name:                         "Test Execution: HSR - Enable replication on primary"
      when:                         database_high_availability | bool
      block:

        - name:                     "Test Execution: HSR - Enable replication on primary"
          become_user:              "{{ db_sid | lower }}adm"
          ansible.builtin.shell: |
                                    set -o pipefail
                                    source /usr/sap/{{ db_sid | upper }}/home/.sapenv.sh && \
                                    hdbnsutil -sr_enable --name={{ cluster_status_pre.primary_site_name | default(backup_hsr_primary_site | default('site1')) }}
          args:
            executable:             /bin/bash
          register:                 hsr_enable_result
          delegate_to:              "{{ backup_primary_node }}"
          run_once:                 true
          changed_when:             hsr_enable_result.rc == 0
          failed_when:              hsr_enable_result.rc != 0
          when:                     (hana_process_check.rc | default(1)) in [0, 3]

        - name:                     "Test Execution: HSR - Start HANA on secondary"
          become_user:              "{{ db_sid | lower }}adm"
          ansible.builtin.shell: |
                                    set -o pipefail
                                    source /usr/sap/{{ db_sid | upper }}/home/.sapenv.sh && \
                                    HDB start
          args:
            executable:             /bin/bash
          register:                 hsr_secondary_start
          delegate_to:              "{{ backup_secondary_node }}"
          run_once:                 true
          changed_when:             hsr_secondary_start.rc == 0
          failed_when:              false
          when:                     hsr_enable_result.rc | default(1) == 0

        - name:                     "Test Execution: HSR - Register secondary for replication"
          become_user:              "{{ db_sid | lower }}adm"
          ansible.builtin.shell: |
                                    set -o pipefail
                                    source /usr/sap/{{ db_sid | upper }}/home/.sapenv.sh && \
                                    hdbnsutil -sr_register \
                                      --name={{ backup_hsr_secondary_site | default('site2') }} \
                                      --remoteHost={{ backup_primary_node }} \
                                      --remoteInstance={{ db_instance_number }} \
                                      --replicationMode={{ cluster_status_pre.replication_mode | default(backup_hsr_replication_mode | default('syncmem')) }} \
                                      --operationMode={{ cluster_status_pre.operation_mode | default(backup_hsr_operation_mode | default('logreplay')) }}
          args:
            executable:             /bin/bash
          register:                 hsr_register_result
          delegate_to:              "{{ backup_secondary_node }}"
          run_once:                 true
          changed_when:             hsr_register_result.rc == 0
          failed_when:              hsr_register_result.rc != 0
          when:                     hsr_enable_result.rc | default(1) == 0

        - name:                     "Test Execution: HSR - Validate replication state"
          become_user:              "{{ db_sid | lower }}adm"
          ansible.builtin.shell: |
                                    set -o pipefail
                                    source /usr/sap/{{ db_sid | upper }}/home/.sapenv.sh && \
                                    hdbnsutil -sr_state
          args:
            executable:             /bin/bash
          register:                 hsr_state_result
          delegate_to:              "{{ backup_primary_node }}"
          run_once:                 true
          changed_when:             false
          failed_when:              false
          when:                     hsr_register_result.rc | default(1) == 0

        - name:                     "Test Execution: Determine test execution status (HSR)"
          ansible.builtin.set_fact:
            test_execution_status:  "{{ 'PASSED' if (
                                        restore_job_status.restore_job.status | default('') | lower == 'completed'
                                        and (hana_process_check.rc | default(1)) in [0, 3]
                                        and hsr_register_result.rc | default(1) == 0
                                      ) else 'FAILED' }}"
          run_once:                 true

    - name:                         "Test Execution: Determine test execution status (standalone)"
      when:                         not (database_high_availability | bool)
      ansible.builtin.set_fact:
        test_execution_status:      "{{ 'PASSED' if (
                                        restore_job_status.restore_job.status | default('') | lower == 'completed'
                                        and (hana_process_check.rc | default(1)) in [0, 3]
                                      ) else 'FAILED' }}"
      run_once:                     true

    - name:                         "Test Execution: Stop timer"
      ansible.builtin.set_fact:
        test_execution_end:         "{{ now(utc=true, fmt='%Y-%m-%d %H:%M:%S') }}"
      run_once:                     true

    - name:                         "Set test case message and details (standalone)"
      ansible.builtin.set_fact:
        test_case_message_from_test_case: >-
          Restore to DB (OLR):
          {{ restore_job_status.restore_job.status | default('N/A') }},
          HANA DB running: {{ (hana_process_check.rc | default(1)) in [0, 3] }},
          Job ID: {{ restore_to_db_result.restore_job.job_id | default('N/A') }}
        test_case_details_from_test_case:
          topology:                 "Standalone"
          restore_trigger:          "{{ restore_to_db_result }}"
          restore_job_status:       "{{ restore_job_status }}"
          hana_stop:                "{{ hana_stop_result }}"
          hana_start:               "{{ hana_start_result }}"
          hana_process_check:       "{{ hana_process_check }}"
      run_once:                     true
      when:                         not (database_high_availability | bool)

    - name:                         "Set test case message and details (HSR)"
      ansible.builtin.set_fact:
        test_case_message_from_test_case: >-
          Restore to DB (HSR - AlternateWorkloadRestore):
          {{ restore_job_status.restore_job.status | default('N/A') }},
          HANA DB running: {{ (hana_process_check.rc | default(1)) in [0, 3] }},
          HSR re-established: {{ (hsr_register_result.rc | default(1)) == 0 }},
          Job ID: {{ restore_to_db_result.restore_job.job_id | default('N/A') }}
        test_case_details_from_test_case:
          topology:                 "HSR"
          restore_trigger:          "{{ restore_to_db_result }}"
          restore_job_status:       "{{ restore_job_status }}"
          hana_stop:                "{{ hana_stop_result }}"
          hana_start:               "{{ hana_start_result }}"
          hana_process_check:       "{{ hana_process_check }}"
          cluster_status_pre:       "{{ cluster_status_pre | default({}) }}"
          hsr_disable:              "{{ hsr_disable_result | default({}) }}"
          hsr_unregister:           "{{ hsr_unregister_result | default({}) }}"
          hsr_enable:               "{{ hsr_enable_result | default({}) }}"
          hsr_register:             "{{ hsr_register_result | default({}) }}"
          hsr_state:                "{{ hsr_state_result | default({}) }}"
      run_once:                     true
      when:                         database_high_availability | default(false) | bool

    - name:                         "Post Validations Tasks"
      ansible.builtin.include_tasks: "roles/backup_db_hana/tasks/post-validations-backup.yml"
      run_once:                     true

  rescue:
    - name:                             "Rescue operation"
      ansible.builtin.include_tasks:    "roles/backup_db_hana/tasks/rescue-backup.yml"
      run_once:                         true

- name:                                 "Pre Validations: Test Case Skipped"
  when:                                 pre_validations_status | default('FAILED') == "FAILED"
  block:
    - name:                             "Set test case message and details"
      ansible.builtin.set_fact:
        test_case_message_from_test_case: "Pre Validations for Azure Backup restore-to-database failed. No protected items found."
        test_case_details_from_test_case:
          pre_validation:               "{{ pre_backup_discovery | default({}) }}"
      run_once:                         true

    - name:                             "Post Validations Tasks"
      ansible.builtin.include_tasks:    "roles/backup_db_hana/tasks/post-validations-backup.yml"
      run_once:                         true
